/** Default Codex model used when callers do not specify one. */
export const DEFAULT_CODEX_MODEL = "gpt-5.2-codex";

/** Default sandbox mode for SDK-backed runs. */
export const DEFAULT_SANDBOX_MODE = "read-only";

/** Default approval policy for SDK-backed runs. */
export const DEFAULT_APPROVAL_POLICY = "never";
