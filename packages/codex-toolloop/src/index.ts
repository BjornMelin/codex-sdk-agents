/**
 * Bootstrap export surface for the future `@codex-toolloop/core` runtime.
 *
 * SPEC 000 requires the workspace/package structure to exist even before the
 * full agent runtime is implemented.
 */
export const CORE_PACKAGE_ID = "@codex-toolloop/core";
