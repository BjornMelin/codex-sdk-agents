{
  "name": "@codex-toolloop/cli",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "pnpm -s -C ../.. lint",
    "test": "pnpm -s -C ../.. test"
  },
  "devDependencies": {
    "tsx": "^4.21.0"
  }
}
